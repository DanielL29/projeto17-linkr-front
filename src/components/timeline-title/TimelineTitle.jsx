import { TimelineTitleWrapper } from "./TimelineTitleStyle";

export default function TimelineTitle({ title }) {
    return (
        <TimelineTitleWrapper>
            {title}
        </TimelineTitleWrapper>
    )
}